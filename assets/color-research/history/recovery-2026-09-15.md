# 历史恢复摘要（2026-09-15）

以下保留原 `assets/color-research/recovery/README.md` 的事实摘要。详细恢复文件和生成原件现为本地素材，不随 Git 交付；下文文件名均相对原恢复目录，绝对路径仅用于追溯。测试数量、工作目录和完成状态为当时快照，不代表当前验证或三张小样的后续颜色验收。当前规则见 [设色流程](../WORKFLOW.md)。

---

# 200 处古迹设色图恢复与完成记录

2026-09-15，在任务 `01a0a4c4-2eae-7fd3-ab2e-c03000698012` 中接续原任务 `01a09e2d-a458-73d0-bb60-eb2b3a86e6bd`。

- 恢复前已有 122 张完成（含三张小样），旧汇总表只记录了 109 张。
- 找回并复核 7 张未入库生成原件，再用内置 imagegen 并行补画 71 张。
- 最终 197 张批量图版加 3 张既有小样，共 200 / 200；逐项研究记录、提示词、实拍来源、生成原件路径均已保存。
- 已核对全部 197 张批量输出与原件 SHA256 一致，672 个输入路径存在；尺寸符合原线稿（沿用 2 像素容差）。
- 38 项现有测试通过；浏览器总览显示 200 / 200，搜索、线稿/设色切换、对照详情与来源链接可用。

## 路径

实际项目：`/Users/fuxiangyu/Workspace/fanggu`。

批量图片：`assets/colored/`；三张小样：`assets/color-studies/v1/`；完整提示词：`assets/color-research/prompts.json`；逐项研究：`assets/color-research/<id>.json`。

`completion.json` 是最终完整性核验；`final-tests.tap` 是测试输出。`final-audit.json` 为中途 185 张快照及来源分类核验，最终数量以 `completion.json` 和重新生成的 `progress.json` 为准。各 worker 文件记录恢复、续画与分组复核。

旧会话记录约 930 MB，读取任务接口报错且最后两次执行被中断；本次未确诊或修改 Codex 本身。恢复只依赖已保存的项目文件、流式提取的必要文字记录和生成原件。后续按 WORKFLOW.md 在生成前保存 prepared，返回后保存 needs_review，再核验为 complete。
